{
    "scripts": {
        "generate": "drizzle-kit generate --dialect sqlite --schema ./server/database/schema.ts"
    },
    "dependencies": {
        "drizzle-kit": "^0.22.8",
        "drizzle-orm": "^0.31.2"
    }
}
